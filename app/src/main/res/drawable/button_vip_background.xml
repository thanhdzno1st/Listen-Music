<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Gradient cho nền của Button -->
    <gradient
        android:startColor="#CCB700"
        android:endColor="#FFF278"
        android:angle="45" />

    <!-- Bo tròn góc -->
    <corners android:radius="50dp" />

    <!-- Viền cho Button -->
    <stroke
        android:width="1dp"
        android:color="#FF007A" />
</shape>